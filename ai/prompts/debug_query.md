The following LogScale query is failing.

Your task:
- Identify root cause
- Classify issue:
  - syntax
  - unsupported function
  - wrong field
  - aggregation issue
  - pipeline issue
- Fix it
- Provide a simplified debug version

Query:
```cql
{{QUERY}}
```

Error:
```text
{{ERROR}}
```

Schema / Fields:
{{FIELDS}}

Expected behavior:
{{EXPECTED}}
